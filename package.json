{
  "name": "redis-jwt-auth",
  "version": "1.0.0",
  "description": "JWT authentication utilities with access & refresh tokens, Redis or memory store, and Express middleware.",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "auth",
    "jwt",
    "redis",
    "tokens"
  ],
  "author": "Akshat Jain",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "express": "^5.1.0",
    "ioredis": "^5.7.0",
    "jsonwebtoken": "^9.0.2"
  }
}
